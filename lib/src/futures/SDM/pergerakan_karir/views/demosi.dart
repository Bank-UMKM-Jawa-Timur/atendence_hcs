import 'package:flutter/material.dart';

class DemosiPage extends StatelessWidget {
  const DemosiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DemosiPage"),
      ),
    );
  }
}
