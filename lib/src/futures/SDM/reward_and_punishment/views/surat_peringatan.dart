import 'package:flutter/material.dart';

class SuratPeringatan extends StatelessWidget {
  const SuratPeringatan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("SuratPeringatan"),
      ),
    );
  }
}
