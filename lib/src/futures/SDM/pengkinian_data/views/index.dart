import 'package:flutter/material.dart';

class PengkinianDataPage extends StatelessWidget {
  const PengkinianDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PengkinianDataPage"),
      ),
    );
  }
}
