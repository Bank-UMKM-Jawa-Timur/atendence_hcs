import 'package:flutter/material.dart';

class DataMasaPensiunPage extends StatelessWidget {
  const DataMasaPensiunPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("DataMasaPensiunPage"),
      ),
    );
  }
}
