import 'package:flutter/material.dart';

class PejabatSementaraPage extends StatelessWidget {
  const PejabatSementaraPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PejabatSementaraPage"),
      ),
    );
  }
}
